<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.libraryms.mapper.BookCategoryMapper">

  <resultMap id="BaseMap" type="com.example.libraryms.entity.BookCategory">
    <id column="id" property="id"/>
    <result column="name" property="name"/>
    <result column="subject" property="subject"/>
    <result column="description" property="description"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <select id="findById" resultMap="BaseMap">
    select * from book_category where id = #{id} limit 1
  </select>

  <select id="findByName" resultMap="BaseMap">
    select * from book_category where name = #{name} limit 1
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    insert into book_category (name, subject, description)
    values (#{name}, #{subject}, #{description})
  </insert>

  <update id="update">
    update book_category
    set
      name = #{name},
      subject = #{subject},
      description = #{description}
    where id = #{id}
  </update>

  <delete id="deleteById">
    delete from book_category where id = #{id}
  </delete>

  <select id="listAll" resultMap="BaseMap">
    select * from book_category order by id desc
  </select>

  <sql id="whereSql">
    <where>
      <if test="subject != null and subject != ''">
        and subject = #{subject}
      </if>
      <if test="keyword != null and keyword != ''">
        and (name like concat('%', #{keyword}, '%') or subject like concat('%', #{keyword}, '%'))
      </if>
    </where>
  </sql>

  <select id="count" resultType="long">
    select count(1) from book_category
    <include refid="whereSql"/>
  </select>

  <select id="page" resultMap="BaseMap">
    select * from book_category
    <include refid="whereSql"/>
    order by id desc
    limit #{limit} offset #{offset}
  </select>

</mapper>

